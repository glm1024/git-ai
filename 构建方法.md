VsCode 打包（git ai 根目录执行）：

```json
sh scripts/offline-build/build-vscode.sh
```

Jetbrains 打包（git ai 根目录执行）：

```json
sh scripts/offline-build/build-jetbrains.sh
```

Cli 重新构建并安装：

```json
cd /Users/mark/workspace/ai-coding/git-ai
bash scripts/dev.sh --release
```
