# Git AI 构建方法

## 插件打包

在 Git AI 根目录执行：

```sh
sh scripts/offline-build/build-plugins.sh
```

打出的安装包会放在 `plugins/` 目录。

## Mac 重新构建并安装 CLI

在 Git AI 根目录执行：

```sh
bash scripts/dev.sh
```

## 构建 Windows CLI 离线安装包

在 Git AI 根目录执行：

```sh
sh scripts/offline-build/build-win.sh
```

## 构建 Linux CLI 离线安装包

在 Git AI 根目录执行：

```sh
sh scripts/offline-build/build-linux.sh
```

最终只需复制 `offline-dist/git-ai-linux-v<CLI version>.tar.gz`。该包同时包含
Linux x64 和 ARM64 CLI，不包含 IDE 插件。

## 研发内网安装和升级

PowerShell 进入解压后的目录执行：

```powershell
.\install.ps1
git-ai --version
```

Linux 使用开发者本人账号解压后执行：

```sh
tar -xzf git-ai-linux-v1.6.25.tar.gz
cd git-ai-linux-v1.6.25
bash ./install.sh
git-ai --version
```

安装脚本会自动识别服务器架构、选择包内对应 CLI 并完成内部校验，用户无需
手动执行 `sha256sum`。
