# Git AI 构建方法

## 插件打包

在 Git AI 根目录执行：

```sh
sh scripts/offline-build/build-vscode.sh
sh scripts/offline-build/build-jetbrains.sh
```

## Mac 重新构建并安装 CLI

在 Git AI 根目录执行：

```sh
bash scripts/dev.sh
```

## 构建 Windows CLI 离线安装包

在 Git AI 根目录执行：

```sh
sh scripts/offline-build/build-win.sh
```

## 研发内网安装和升级

PowerShell 进入解压后的目录执行：

```powershell
.\install.ps1
git-ai --version
```
