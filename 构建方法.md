# Git AI 构建方法

## 插件打包

在 Git AI 根目录执行：

```sh
sh scripts/offline-build/build-vscode.sh
sh scripts/offline-build/build-jetbrains.sh
```

## 本机（Mac）重新构建并安装 CLI

在 Git AI 根目录执行：

```sh
bash scripts/dev.sh
```

## 构建研发内网使用的 Windows CLI

Windows exe 在当前机器构建，研发内网只安装。在 Git AI 根目录执行：

```sh
sh scripts/offline-build/build-win.sh
```

最终输出一个文件：

```text
offline-dist/git-ai-windows-v<CLI版本>.zip
```

把这个 ZIP 复制到研发内网并完整解压。

## 研发内网安装和升级

在执行：

```powershell
$env:GIT_AI_LOCAL_BINARY = (Resolve-Path -LiteralPath 'D:\git-ai-windows-v<CLI版本>\windows\git-ai-windows-x64.exe').Path
& 'C:\git-ai-installer\install.ps1'
Remove-Item Env:GIT_AI_LOCAL_BINARY
git-ai --version
```

后续升级只需重新执行 `build-win.sh`，把新 ZIP 复制到研发内网并解压，再把命令中的 exe 路径改成新版本。`install.ps1` 继续使用固定的同一份；只有安装脚本自身需要修复时才更新。
