# Git AI 产品业务档案

## 业务目标

Git AI 是面向使用 AI 编码工具的研发团队的 Git 扩展，目标是在不改变日常提交流程的前提下，记录代码中哪些行由 AI 辅助生成，以及这些行对应的工具、模型、会话和提示上下文。产品重点不是事后猜测代码是否由 AI 生成，而是在代码产生和提交过程中采集来源事实，并让这些来源信息随 Git 历史一起流转。

## 核心角色

- 开发者：正常使用 AI 编码工具和 Git 提交代码，希望事后能理解某段代码的生成背景。
- 团队负责人和工程效能角色：关注 AI 代码采纳率、保留率、模型/工具效果和团队实践差异。
- 安全、合规和审计角色：需要知道 AI 参与了哪些代码变更，以及相关上下文是否可追溯、可控、可审查。

## 主要流程

1. AI 编码工具或编辑器在代码变更发生时触发检查点，记录文件内容、来源会话和模型等上下文。
2. 提交时将检查点汇总为行级作者来源记录，并作为 Git 附加元数据关联到提交。
3. 查询时通过统计、类 blame 视图或上下文问答，查看每行代码背后的 AI 工具、模型、会话和提示。
4. 当发生 rebase、squash、cherry-pick、stash、reset、amend 等 Git 历史改写或工作区迁移操作时，系统尝试让 AI 来源信息随代码内容迁移，而不是只停留在原始提交上。

## 关键业务规则

- 来源记录以实际采集事实为准，不把“检测 AI 代码”作为主要口径。
- 开源本地模式应尽量本地优先，避免把代码、提示或会话默认上传到外部服务。
- 提交历史不应被来源记录污染，来源信息应作为 Git 原生附加数据存在。
- 行级来源信息要能区分 AI、已知人工编辑和未知来源，不把未知来源简单等同于人工或 AI。
- 团队/企业场景可以在权限控制和脱敏基础上同步提示、会话和统计数据，用于审计、度量和协作。
- 数据上报配置在 VS Code 与 JetBrains 端保持同一份 Git AI CLI 配置。插件优先保留 Git AI 已有字段，仅在字段为空时从本机 Kilo 配置补齐；不会回写 Kilo。
- VS Code 数据上报页的页头标题始终保持单行展示；成功、未保存、缓存或错误状态只占用标题与保存按钮之间的剩余空间，长状态文案省略显示但保留完整提示，不能挤压标题或保存操作。
- 用户修改上报服务器地址后会重新加载组织架构；加载失败时保留当前部门、处、组和值，不把“服务不可用”误判成“当前处没有组”。新服务器成功返回后如果原选择已失效，应提示用户重新选择；只有确认当前处本身不设组时，保存时才清除旧组值。
- 数据上报页对空地址、格式错误、连接失败、超时、接口不存在、权限不足、服务异常和响应格式异常统一显示短提示“上报地址不可用”，不在紧凑页头暴露 HTTP 状态或技术原因；详细诊断写入 VS Code 插件日志，供排查时使用。
- 组织架构由用户填写的上报服务器提供，客户端可缓存最近一次成功结果以应对短暂不可用，但保存时仍按当前可用组织架构校验选择值。
- AI checkpoint 的新增行数与删除行数属于编辑过程事实；企业统计可以据此计算接受行数，这类事实不因后续 Git 历史改写回退。
- 普通提交和改写后的提交都上报最终 authorship note 与 hunks；release 版本默认上报 rewrite committed 事件，后台仍需根据目标分支确认后再决定是否形成正式入库统计。
- authorship note 写入后，Event 1 的完整证据不得因合并提交、计算异常、hunk 过多或单条上传包过大而丢失。普通小提交继续走原有即时事件；513 个及以上 hunk、达到 7 MiB 上限、合并提交或即时计算失败时，改为写入本地 SQLite 延迟任务，重启后按租约回收和退避继续处理。延迟任务把每块限制在 512 个 hunk 且严格小于上传上限，所有分块在同一事务进入 outbox，并携带稳定 bundle 标识、序号、总数和完整 hunk 摘要；整个 bundle 最多 128 块、65,536 个 hunk、64 MiB hunk JSON 和 64 MiB 事件 JSON，超限或单个 hunk 无法装入一个事件时保留任务和诊断，不生成缺少 hunks 的不完整 Event 1。每个 Event 1 只描述一个最终提交，并以该提交真实的第一父提交为 base；即使 daemon 观察到跨多个提交的 fast-forward，也不能把变更前的旧分支 tip 当成这次提交的父提交。根提交统一相对 Git 空树计算，客户端所有新事件中的 base commit 都保持为空，不把内部 `initial` 约定值冒充 Git 对象 ID；旧客户端 `initial` 的兼容只由后台负责。任务成功后永久保留最小幂等 tombstone 和已生成事件标识，立即清空路径、作者等载荷；历史 done 行若仍残留旧载荷，则在启动和后台轮询时分批清理。单亲提交的新增行只用当前提交 note 判定，删除行只用提交时快照的父提交 note 和旧侧路径/行号判定；父 note 缺失或无效时删除归因保持未知，不用后来变化的仓库状态补猜。合并提交只统计相对所有父提交都新增、且与 authorship note 中 AI 证明相交的最终结果行，正式 combined diff 固定使用 `--find-renames=50%` 保持重命名身份，不把 merge resolution 中的纯改名误计为 novel AI 行；干净合并不生成 Event 1。
- amend、rebase、reset 和非快进 ref 变更会额外上报强生命周期事件，包含稳定操作 ID、old/new tip、失效提交和替代提交。reset、branch force 等命名分支指针移动先把不可变 old/new 意图写入本地 SQLite，再异步枚举完整 commit 集并按退避重试；每块每侧最多 512 个 commit、整个 bundle 最多 64 块、32,768 个唯一 commit、单块 256 KiB、总计 8 MiB，所有块共享同一时间戳与属性快照。detached HEAD 不代表命名分支变化，不生成会被后台拒绝的生命周期 bundle。cherry-pick、merge squash 和 revert 不得被表达为“源提交失效”。
- 原始 session event 只在内存中解析，不写入指标 SQLite，也不上报。客户端分别识别 Claude、Kilo/OpenCode 和 Codex 的 Token 结构，以稳定消息或会话来源去重绝对计数，只保留时间、工具、模型、仓库、企业 reporting profile、恢复标记和无正文 Token 计数；prompt、回复、推理、工具输入输出和文件内容均被丢弃。
- Token 上报采用“日累计快照”而不是逐消息增量：按日期、用户、稳定安装 ID、项目、IDE、工具、模型提供商和模型合并，同一维度在 8082 不可用时只更新一条待上传快照；上传中的旧快照不原地修改，新数据写入后继快照，确保成功确认不会丢掉并发新增数据。每个快照 generation 使用稳定 UUID，身份重键时显式携带被替代的 instance，不能仅凭复用的 source key 关闭之后产生的新 generation。恢复后重传最新累计值，后台可按新鲜度幂等覆盖，重复或乱序上传不会重复累计。
- Codex 子任务会继承父会话的完整历史和累计 Token。客户端必须先识别当前子任务的真实 fork 边界，将边界前最后一条累计值只作为本子任务来源的基线，并仅统计边界后的增量；边界尚未完整写入时不推进读取水位，也不把继承历史当成新增用量。修正后的 Codex 日快照使用独立投影修订版本，避免与旧错误快照相加，并允许后台以新修订的较小正确值替换旧修订的膨胀值。
- 客户端按 7 MiB 请求预算和最多 1000 条记录双重分批，只锁定本次实际入选的记录。认证、地址配置和请求体过大等可修复错误不做无意义的 60 秒即时重试，而是保留本地事实并进入最长 24 小时的持续退避；明确的请求语义错误停止自动重试但仍保留本地诊断记录，不静默删除提交事实。
- 本地恢复标记保留 7 天，紧凑会话和 Token 来源事实保留 365 天；已成功上传的 Token 快照立即从 outbox 删除。指标库升级到 v8 时，会分批将旧 session/OTEL 原始事件投影为同样的无正文事实并移除原始行，并为 Codex fork 来源单独保存继承基线；迁移可中断续跑且不对真实库自动执行高临时空间消耗的完整 `VACUUM`。因此新增长被阻断且旧正文会被逻辑移除，但既有数据库文件不保证立即物理缩小，物理空间回收仍需先备份并执行受控维护。
- Kilo v7 作为 Git AI 的一等采集工具接入，覆盖 VS Code、JetBrains 与 Kilo CLI 中经 Kilo Agent 工具产生的 edit、write、patch 和 bash/shell 变更；三端统一保留 `kilo` 工具身份，并分别记录运行端、编辑器、Kilo 版本、模型与模型提供商。Tab/自动补全不在本次统计范围内。
- Kilo v7 插件由 Git AI 现有 OpenCode 插件模板在安装时通过少量、严格校验的替换生成，Kilo 路径、身份与运行维度使用独立 preset；后续合并 Git AI 上游时优先继承 OpenCode 公共修复，不长期维护另一份大体量插件副本。
- Kilo v7 的 Git AI 指标上线后直接进入后台 `official` 投影，不要求先运行 shadow；现有模式开关只作为故障时暂停正式投影的运维回退能力，不删除原始事件、Git Notes 或已确认事实。

## 重要边界

- 产品价值依赖 AI 工具、编辑器或 Git 流程中能采集到足够来源事实；未被采集的变更只能作为未知来源处理。
- Kilo v7 只能可靠统计内置编辑/写入/补丁工具和 bash/shell 已暴露的文件变更；无法暴露文件路径的工具以及绕过这些 Hook 的任意 MCP 文件写入按未知来源处理，不事后猜测。
- Git 历史改写、服务端合并和文件重命名等场景会影响行级来源连续性，需要专门处理才能保持准确。
- 生命周期事件采用至少一次上传语义，服务端必须按操作 ID、分块序号和原始事件键幂等处理，并能够容忍生命周期事件早于或晚于提交事件到达。
- `git-ai await` 读取本地延迟任务或指标 outbox 失败时必须按“仍有待处理数据”处理，不能把数据库读取错误折算为 0 并对用户宣称已经清空。
- 公开仓库和公开文档本身可能构成专利申请前公开披露；涉及专利布局时应尽快由专利代理人确认公开时间、申请地域和可主张范围。
