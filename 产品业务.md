# Git AI 产品业务档案

## 业务目标

Git AI 是面向使用 AI 编码工具的研发团队的 Git 扩展，目标是在不改变日常提交流程的前提下，记录代码中哪些行由 AI 辅助生成，以及这些行对应的工具、模型、会话和提示上下文。产品重点不是事后猜测代码是否由 AI 生成，而是在代码产生和提交过程中采集来源事实，并让这些来源信息随 Git 历史一起流转。

## 核心角色

- 开发者：正常使用 AI 编码工具和 Git 提交代码，希望事后能理解某段代码的生成背景。
- 团队负责人和工程效能角色：关注 AI 代码采纳率、保留率、模型/工具效果和团队实践差异。
- 安全、合规和审计角色：需要知道 AI 参与了哪些代码变更，以及相关上下文是否可追溯、可控、可审查。

## 主要流程

1. AI 编码工具或编辑器在代码变更发生时触发检查点，记录文件内容、来源会话和模型等上下文。
2. 提交时将检查点汇总为行级作者来源记录，并作为 Git 附加元数据关联到提交。
3. 查询时通过统计、类 blame 视图或上下文问答，查看每行代码背后的 AI 工具、模型、会话和提示。
4. 当发生 rebase、squash、cherry-pick、stash、reset、amend 等 Git 历史改写或工作区迁移操作时，系统尝试让 AI 来源信息随代码内容迁移，而不是只停留在原始提交上。

## 关键业务规则

- 来源记录以实际采集事实为准，不把“检测 AI 代码”作为主要口径。
- 开源本地模式应尽量本地优先，避免把代码、提示或会话默认上传到外部服务。
- 提交历史不应被来源记录污染，来源信息应作为 Git 原生附加数据存在。
- 行级来源信息要能区分 AI、已知人工编辑和未知来源，不把未知来源简单等同于人工或 AI。
- 团队/企业场景可以在权限控制和脱敏基础上同步提示、会话和统计数据，用于审计、度量和协作。
- 数据上报配置在 VS Code、JetBrains 与 CLI 端共用同一份 Git AI CLI 配置。Git AI 路线的姓名、企业邮箱、部门、处和组只来自完整的 `reporting_profile`；不得从 Kilo 登录信息、操作系统账号、Git author 或通用自定义属性补齐正式人员与组织身份。项目仍按 Git remote 等可验证仓库信息自动识别，Git author 只作为提交事实和已配置用户的关联证据。
- 企业上报采用“内网可达 + Git AI 配置身份可信”的既定模型：独立 metrics 地址不携带 Git AI 登录凭据，也不另加设备凭据或请求签名；新客户端的完整 `reporting_profile` 携带专用来源版本标记，服务端只把这一规范形态用于正式归属。为兼容已经发布的旧客户端，标记缺失但同时具备规范企业邮箱、姓名、部门和处字段的旧 `reporting_profile` 仍可识别；未知标记、别名邮箱、Git author 或不完整字段均不能通过身份门禁。缺少完整配置的事实继续保留为身份待补全；客户端不得用进程当前的全局配置批量认领历史匿名 Token。匿名 Event 9 按会话隔离，只有同一会话首次出现可信且未发生冲突的配置身份时，才可恢复该会话此前的匿名事实；同一会话出现 A、B 两个不同身份后，中间匿名事实保持待补全，已经归属于 A 的事实绝不能改成 B。升级前无法证明会话身份来源的紧凑记录保持历史归属不明；旧记录自身携带的完整规范 `reporting_profile`（包括尚无来源版本标记的旧规范形态）仍保持原身份。该边界接受可访问采集地址的内网调用方伪造上报身份或数值的风险，客户端和服务端的 ACK、幂等、字段及流量校验不得被表述为身份认证。
- VS Code 数据上报页的页头标题始终保持单行展示；成功、未保存、缓存或错误状态只占用标题与保存按钮之间的剩余空间，长状态文案省略显示但保留完整提示，不能挤压标题或保存操作。
- 用户修改上报服务器地址后会重新加载组织架构；加载失败时保留当前部门、处、组和值，不把“服务不可用”误判成“当前处没有组”。新服务器成功返回后如果原选择已失效，应提示用户重新选择；只有确认当前处本身不设组时，保存时才清除旧组值。
- 数据上报页对空地址、格式错误、连接失败、超时、接口不存在、权限不足、服务异常和响应格式异常统一显示短提示“上报地址不可用”，不在紧凑页头暴露 HTTP 状态或技术原因；详细诊断写入 VS Code 插件日志，供排查时使用。
- 组织架构由用户填写的上报服务器提供，客户端可缓存最近一次成功结果以应对短暂不可用，但保存时仍按当前可用组织架构校验选择值。
- AI checkpoint 的新增行数与删除行数属于编辑过程事实；企业统计可以据此计算接受行数，这类事实不因后续 Git 历史改写回退。
- 文件编辑工具的执行前 checkpoint 必须按工具输入中的精确文件范围保存当前工作区快照，执行后的 AI checkpoint 只能相对同一次工具调用的执行前快照计算增删行。即使工作区正处于 merge、rebase 或 cherry-pick，第二父、索引或此前人工处理已经带入的代码也不得因首个 AI checkpoint 缺少历史基线而整包归给当前模型；无法取得执行前文件范围或快照时应保守失败或保持未知，不能退回第一父 HEAD 后把累计工作区差异当作本次 AI 编辑。
- 普通提交和改写后的提交都上报最终 authorship note 与 hunks；release 版本默认上报 rewrite committed 事件，后台仍需根据目标分支确认后再决定是否形成正式入库统计。企业后台只关注已经配置有效 Git remote 的项目，但不维护仓库白名单：任何首次出现且远程地址可以规范识别的项目代码事实都允许上报；没有远程地址、远程地址为空或无法识别的普通提交、Checkpoint、改写提交和分支生命周期事件只保留在本地，不发往企业后台，也不进入上传重试。仓库以后新增远程地址时仅从后续事件开始上报，不补传此前的本地项目事实；不绑定具体仓库的工具使用和 Token 事实不受该门禁影响。
- authorship note 写入后，Event 1 的完整证据不得因合并提交、计算异常、hunk 过多或单条上传包过大而丢失。普通小提交继续走原有即时事件；513 个及以上 hunk、达到 7 MiB 上限、合并提交或即时计算失败时，改为写入本地 SQLite 延迟任务，重启后按租约回收和退避继续处理。延迟任务把每块限制在 512 个 hunk 且严格小于上传上限，所有分块在同一事务进入 outbox，并携带稳定 bundle 标识、序号、总数和完整 hunk 摘要；整个 bundle 最多 128 块、65,536 个 hunk、64 MiB hunk JSON 和 64 MiB 事件 JSON，超限或单个 hunk 无法装入一个事件时保留任务和诊断，不生成缺少 hunks 的不完整 Event 1。每个 Event 1 只描述一个最终提交，并以该提交真实的第一父提交为 base；即使 daemon 观察到跨多个提交的 fast-forward，也不能把变更前的旧分支 tip 当成这次提交的父提交。根提交统一相对 Git 空树计算，客户端所有新事件中的 base commit 都保持为空，不把内部 `initial` 约定值冒充 Git 对象 ID；旧客户端 `initial` 的兼容只由后台负责。任务成功后永久保留最小幂等 tombstone 和已生成事件标识，立即清空路径、作者等载荷；历史 done 行若仍残留旧载荷，则在启动和后台轮询时分批清理。单亲提交的新增行只用当前提交 note 判定，删除行只用提交时快照的父提交 note 和旧侧路径/行号判定；父 note 缺失或无效时删除归因保持未知，不用后来变化的仓库状态补猜。合并提交只统计相对所有父提交都新增、且与 authorship note 中 AI 证明相交的最终结果行，正式 combined diff 固定使用 `--find-renames=50%` 保持重命名身份，不把 merge resolution 中的纯改名误计为 novel AI 行；干净合并不生成 Event 1。
- amend、rebase、reset 和非快进 ref 变更会额外上报强生命周期事件，包含稳定操作 ID、old/new tip、失效提交和替代提交。reset、branch force 等命名分支指针移动先把不可变 old/new 意图写入本地 SQLite，再异步枚举完整 commit 集并按退避重试；每块每侧最多 512 个 commit、整个 bundle 最多 64 块、32,768 个唯一 commit、单块 256 KiB、总计 8 MiB，所有块共享同一时间戳与属性快照。detached HEAD 不代表命名分支变化，不生成会被后台拒绝的生命周期 bundle。cherry-pick、merge squash 和 revert 不得被表达为“源提交失效”。
- 原始 session event 只在内存中解析，不写入指标 SQLite，也不上报。客户端分别识别 Claude、Kilo/OpenCode 和 Codex 的 Token 结构，以稳定消息或会话来源去重绝对计数，只保留时间、工具、模型、仓库、企业 reporting profile、恢复标记和无正文 Token 计数；prompt、回复、推理、工具输入输出和文件内容均被丢弃。
- Token 上报采用“日累计快照”而不是逐消息增量：按日期、用户、稳定安装 ID、项目、IDE、工具、模型提供商和模型合并，同一维度在 8082 不可用时只更新一条待上传快照；上传中的旧快照不原地修改，新数据写入后继快照，确保成功确认不会丢掉并发新增数据。每个快照 generation 使用稳定 UUID，身份重键时显式携带被替代的 instance，不能仅凭复用的 source key 关闭之后产生的新 generation。恢复后重传最新累计值，后台可按新鲜度幂等覆盖，重复或乱序上传不会重复累计。
- Codex 子任务会继承父会话的完整历史和累计 Token。客户端必须先识别当前子任务的真实 fork 边界，将边界前最后一条累计值只作为本子任务来源的基线，并仅统计边界后的增量；边界尚未完整写入时不推进读取水位，也不把继承历史当成新增用量。修正后的 Codex 日快照使用独立投影修订版本，避免与旧错误快照相加，并允许后台以新修订的较小正确值替换旧修订的膨胀值。
- 客户端按 7 MiB 请求预算和最多 1000 条记录双重分批，只锁定本次实际入选的记录。每条新指标事件在首次形成时生成稳定实例标识并随事件一起写入 SQLite，重试和重启不得重建该标识；旧 SQLite 事件保持旧协议形态，不在上传时临时补标识。后台会在能够证明旧事件原文一致时永久补充规范事件摘要；如果旧事件原文已被清理、原始字节又因字段顺序变化而无法证明是否为同一事实，后台必须逐项拒绝，客户端继续保留该 outbox 供后续恢复，不能因一次模糊重试形成重复统计。认证、地址配置和请求体过大等可修复错误不做无意义的 60 秒即时重试，而是保留本地事实并进入最长 24 小时的持续退避；明确的请求语义错误停止自动重试但仍保留本地诊断记录，不静默删除提交事实。
- 本地恢复标记保留 7 天，紧凑会话和 Token 来源事实保留 365 天；已成功上传的 Token 快照立即从 outbox 删除。指标库升级到 v8 时，会分批将旧 session/OTEL 原始事件投影为同样的无正文事实并移除原始行，并为 Codex fork 来源单独保存继承基线；迁移可中断续跑且不对真实库自动执行高临时空间消耗的完整 `VACUUM`。因此新增长被阻断且旧正文会被逻辑移除，但既有数据库文件不保证立即物理缩小，物理空间回收仍需先备份并执行受控维护。
- Kilo v7 作为 Git AI 的一等采集工具接入，覆盖 VS Code、JetBrains 与 Kilo CLI 中经 Kilo Agent 工具产生的 edit、write、patch 和 bash/shell 变更；三端统一保留 `kilo` 工具身份，并分别记录运行端、编辑器、Kilo 版本、模型与模型提供商。Tab/自动补全不在本次统计范围内。
- Kilo v7 插件由 Git AI 现有 OpenCode 插件模板在安装时通过少量、严格校验的替换生成，Kilo 路径、身份与运行维度使用独立 preset；后续合并 Git AI 上游时优先继承 OpenCode 公共修复，不长期维护另一份大体量插件副本。
- OpenCode/Kilo v7 在执行会修改仓库的工具前，必须同步取得 Git AI 后台已应用 checkpoint 的确认；缺少 CLI、超时、解析失败、后台拒绝或关联标识缺失时，Hook 必须明确报错并阻止该次操作，不能静默放行。每次调用按集成、会话、调用、阶段和仓库形成稳定任务键，先把完整请求、身份快照和规范化文件范围写入本地持久队列，再按仓库顺序应用 working log 与指标 outbox；重启、租约回收和确认丢失只能经同一仓库顺序门禁重放同一任务，不能重复生成事实。恢复时只有仓库身份和所有冻结基线仍与当前状态一致才可执行；损坏或缺失的 `INITIAL` 证据、无法验证的仓库和已经移动的基线必须保留为可见阻断状态，同仓库后续任务不得越过，其他仓库可以继续，人工修复前必须先备份再重置基线。工具执行后的文件范围只能等于或缩小已确认的执行前范围；Bash 因执行前快照覆盖全仓可使用明确的通配范围。checkpoint 确认成功后才能清除本次调用关联；失败状态不得显示为成功。单次调用包含多个仓库时按仓库分别持久化，Git 仓库外路径不应阻断仓库内事实，也不能被误归到默认工作区。
- Kilo VS Code 扩展已安装但尚未首次启动 Kilo 时，Git AI 安装检测仍应识别该客户端并把 Hook 写入 Kilo 官方全局插件目录；不能要求用户先手动运行 Kilo 生成配置目录。
- Kilo v7 的 Git AI 指标上线后直接进入后台 `official` 投影，不要求先运行 shadow；现有模式开关只作为故障时暂停正式投影的运维回退能力，不删除原始事件、Git Notes 或已确认事实。
- 离线交付包必须使用当前同一个干净 commit 重新构建全部 CLI 与 IDE 产物；每个产物保留源 commit、源码干净状态和 SHA-256，包级校验清单还必须覆盖安装手册、构建元数据和安装脚本。打包前后都要校验，版本号与 CLI 不一致、缺失、旧 commit、脏源码、混合产物或哈希不匹配一律拒绝发布。该机制是防止误发的本地一致性门禁，未包含签名或可验证的供应链身份，不得宣称为加密供应链证明。CLI-only 环境在启动后台或产生正式统计事实前，必须先用开发者本人操作系统账号下发专用统计地址和完整 `reporting_profile`。
- 离线 CLI 安装和升级以二进制入口为事务边界：升级时成组保留旧 `git-ai`、已有 `git` shim 和 Unix CLI 链接，候选文件必须以真实入口 basename 在同文件系统暂存，先持久化恢复日志再移动入口，全部关键步骤成功后才提交并清理备份；普通失败恢复一致旧版，进程强杀或断电后由下次安装先恢复未完成事务，恢复状态存在歧义时停止并保留日志供人工核验。首次失败只移除本次创建的入口。统计 SQLite schema 只向前迁移，安装器默认拒绝 CLI 降级；版本回滚不得删除整个 `.git-ai`，必须先完整备份并保留登录/上报配置、SQLite 本地事实和待上传 outbox。Windows 自更新的后台进程启动只代表已调度；安装前必须把实际获取的 channel tag 和版本严格持久化为 pending，只有新进程确认 receipt 中的发布 tag、预期版本、实际运行版本与 pending cache 完全一致，并成功清除 pending 后，才可记为升级成功。当 pending 版本已与当前运行版本一致但 receipt 缺失时，必须重新获取当前 channel 并校验完整 tag 与版本；channel 回退或 tag 已变更时清除过期 pending，不得为恢复 receipt 周期性重启后台服务。committed 日志必须保留重建 receipt 所需的路径、tag 和版本，断电恢复时先校验当前 binary/shim 再补写 receipt；cache 清理失败必须保留 receipt 和 pending，receipt 清理失败则保留可见告警但不得重复安装。

## 重要边界

- 产品价值依赖 AI 工具、编辑器或 Git 流程中能采集到足够来源事实；未被采集的变更只能作为未知来源处理。
- Kilo v7 只能可靠统计内置编辑/写入/补丁工具和 bash/shell 已暴露的文件变更；无法暴露文件路径的工具以及绕过这些 Hook 的任意 MCP 文件写入按未知来源处理，不事后猜测。一个 shell 调用跨多个仓库写入时，只有执行目录所在仓中被实际观察到的路径可以直接归因；其他仓不能仅凭目录包含关系、文件时间接近或同一会话已在目标提交出现就补成 AI 代码。
- Git 历史改写、服务端合并和文件重命名等场景会影响行级来源连续性，需要专门处理才能保持准确。
- 生命周期事件采用至少一次上传语义，服务端必须按操作 ID、分块序号和原始事件键幂等处理，并能够容忍生命周期事件早于或晚于提交事件到达。
- `git-ai await` 读取本地延迟任务或指标 outbox 失败时必须按“仍有待处理数据”处理，不能把数据库读取错误折算为 0 并对用户宣称已经清空。
- working log 超过安全读取上限时必须保留原文件并明确失败，不能为了恢复运行而清空已有 checkpoint 证据；后续压缩或人工处置也必须以可恢复副本为前提。
- working log 的 `INITIAL` 与 checkpoint 必须引用已经按内容寻址持久化并校验过的文件快照；只有 `INITIAL` 文件确实不存在时才能视为空状态，JSON 损坏、快照映射缺失、blob 缺失或哈希不一致都必须明确失败并保留上一份有效基线。working log 改名、原子替换和空态删除都要完成对应目录持久化门禁，不能在崩溃后把已有归因静默变成空状态或把坏引用标记为处理成功。旧客户端遗留的损坏 `INITIAL` 只能通过显式的 `git-ai repair checkpoint-baseline --job-key <key>` 恢复：后台服务必须先停止，首次调用逐项展示将被放弃的任务、worktree 与 base，并生成确认 ID；只有阻断原因与原仓库中当前损坏的 `INITIAL` 证据明确一致时，确认调用才可先导出完整 deferred 原始证据、再归档整份坏 working log。若阻断来自冻结 request、prepared checkpoint、指标事件或其他非 `INITIAL` 证据，即使原仓库仍可验证，也只能切换到绑定确认 ID 的 `frozen_evidence_only` 模式，将完整 SQLite frozen evidence 导出到 Git AI 全局私有目录并解除 FIFO，原 working log 不得归档、修改或重置。若冻结仓库已永久丢失、无法解析，或原路径已是另一个仓库，则切换为绑定确认 ID 的 `unavailable_repository` 模式，同样先导出全局证据再解除 FIFO；不得读写、删除或覆盖原路径上的现存目录，并必须在预览与 receipt 中明示“原 working log 无法归档，也无法由本次修复恢复”的不可逆证据边界。两类全局模式都只在终态落库后写入或补写 completion receipt；受影响 FIFO 任务记录为可审计的 `manual_abandoned` 终态而不是普通成功，原载荷不得被压缩清除。仅归档损坏 `INITIAL` 的模式会在该 base 再次使用时从空归因基线重建，其他 base 不得被该命令清空。Windows 运行时没有可移植的目录 `fsync` 能力，断电后可能需要重复同一确认命令；重试必须重新核验 SQLite tombstone、外部 evidence、source、archive 和完成 receipt，状态矛盾时明确失败，不能宣称修复成功。
- CLI、VS Code/JetBrains 设置页和安装钩子修改共享配置时，必须使用同一个跨进程锁，在获得锁后重读最新配置，再通过同目录临时文件同步并原子替换；已有配置软链不能被替换为普通文件。手工编辑器或企业配置管理若绕过该锁直接写入，不属于并发互斥承诺范围。
- `research-v1` 指标协议尚未通过研究门禁，后台也没有对应的正式接收端点；在完成协议、收据、pilot 和上线授权前，不得进入默认编译或生产运行链路。
- 公开仓库和公开文档本身可能构成专利申请前公开披露；涉及专利布局时应尽快由专利代理人确认公开时间、申请地域和可主张范围。
